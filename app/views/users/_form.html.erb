     <div class="forms-container">
      <%= simple_form_for @user do |f| %>
        <div class="form-inputs">
          <%= f.input :avatar, as: :file, label: false %>
        </div>
        <div class="form-actions">
          <%= f.button :submit, "Upload", class: "btn-white", id: "sml-upload" %>
        </div>
      <% end %>
    </div>
