<div class="nav">

  <div class="fm_heading">
    <%= image_tag 'fm_heading.png' %>
  </div>

  <div class="nav-links">
    <% if user_signed_in? %>
          <li><%= link_to image_tag('home-btn.png', ), class: "nav-link" %></li>
          <li><%= link_to image_tag('news.png', ), class: "nav-link" %></li>
          <li><%= link_to image_tag('content.png', ), class: "nav-link" %></li>
          <li><%= link_to image_tag('favourites.png', ), class: "nav-link" %></li>
        </div>
    <% else %>
        </li><%= link_to "Enter", new_user_session_path, class: "nav-link" %></li>
    <% end %>

  <div class="burger">
    <div class="line1"></div>
    <div class="line2"></div>
    <div class="line3"></div>
  </div>
</div>

 <script>
   const navSlide = () => {
      const burger = document.querySelector('.burger');
      const nav = document.querySelector('.nav-links');
      const navLinks = document.querySelectorAll('.nav-links li');

      burger.addEventListener('click', () => {
        // Toggle Nav
        nav.classList.toggle('nav-active');

      //Animate Links
      navLinks.forEach((link, index) => {
        if (link.style.animation) {
            link.style.animation = '';
        } else {
          link.style.animation = `navLinkFade 0.5s ease forwards ${index / 7 + 0.5}s`;
        }
      });
    });
  }

  navSlide ();

 </script>

